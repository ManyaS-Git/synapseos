'use client';

/**
 * Theme provider for dark/light mode.
 * TODO: Implement using next-themes
 */
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
