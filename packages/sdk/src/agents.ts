/**
 * Agents module for the SynapseOS SDK.
 *
 * TODO: Implement:
 * - list(): Get all agents
 * - getStatus(id): Get agent status
 * - sendMessage(agentId, message): Send message to agent
 * - stream(agentId, message): Stream agent responses
 */

// TODO: Implement when API is ready
export {};
